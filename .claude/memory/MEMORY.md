# Memory Index

- [Fritsch's Stitches](fritschs-stitches.md) — Kent's company is an embroidery business; assume embroidery domain, ignore wheel/tire context
- [EMB Bot Digitizer](emb-bot-digitizer.md) — embroidery digitizer tool at <repo-root>; 2026-07-29 market-parity-vs-Ember launch roadmap; quality engine state + "flat art in, pro out" rule
- [Ember architecture](ember-architecture.md) — competitor teardown: 2 Next.js apps + iframe, PixiJS/MobX/WASM, no ML in auto-digitize; client-side codec usable as DST reference
- [Ember feature teardown](ember-feature-teardown.md) — 2026-08-09 manual/pricing teardown; fill-pattern lib + Bridge app are biggest gaps; handoff doc in EMB-Bot/docs
- [Fill density convention](fill-density-convention.md) — Law 19 settled: pro density is measured between same-direction rows, so our tatami is 2x light; satin is correct
- [DST codec axis discrepancy](dst-codec-axis-discrepancy.md) — EMB-Bot's JS DST bit table is transposed vs pyembroidery/Tajima standard; unresolved, needs a sew-out
- [Ink/Stitch research](inkstitch-research.md) — 2026-08-10 gap/corroboration sweep; pystitch is 5th DST-axis source + MIT swap candidate; GPL-3.0 caveat on porting
- [Pro trim threshold](pro-trim-threshold.md) — corpus says the pro floats up to 16.1mm uncut vs our trim_at_mm 3.0, but the cut/float distributions overlap: distance is not the decision variable
- [Real-artwork parity](real-artwork-parity.md) — 2026-08-15/16: honest baseline 42.5, a pro scores only 75-84 against a pro, and synthetic references flattered the engine in four places
- [Satin gate attribution](satin-gate-attribution.md) — the DT regularity term loses the pro's satin ground; promotion on `explained` moved the corpus 45.8 to 48.1, and the sub-1mm width floor is disproved for flat art
- [Windows goldens fail locally](windows-goldens-fail-locally.md) — per-fixture golden divergence: Windows fails a different three than CI deselects; main is green, never re-capture a golden locally; OCR tests skip without tesseract
- [Digitizer local env](digitizer-local-env.md) — venv editable since 2026-08-17 (stale site-packages shadowing fixed); system tesseract missing, so the real-read OCR tests skip locally (skipif since PR #165) and run in CI
- [Satin extremity drop and coverage check](satin-extremity-drop-and-coverage-check.md) — enthusiast_logo's satin brackets silently drop tabs/corners despite correct outlines; preflight's new ARTWORK_UNCOVERED check catches it, but ground truth needs polygon ∩ ink — either mask alone has its own false-positive class
- [Real-artwork trim truth](real-artwork-trim-truth.md) — 2026-08-22: on Kent's REAL client logos the only large trim lever is `chain_links` (−33%, and FEWER stitches), gate-1 frozen and awaiting his call; PR #205's fill ordering is byte-identical there and every gate-clear alternative is ≤9%. Real logos are satin-dominated, 1–3 fill shapes that never cut. Read before proposing any trim work
- [Sew-out accepted as-is](sew-out-accepted-as-is.md) — Kent ruled 2026-08-21 the physical sew-out is accepted as-is, NOT a queued to-do; dependent scores are permanently `pending sew-out` and ROADMAP gate 1 is a standing refusal — but it does NOT decide the DST codec fix or `split_tonal_regions`, both of which stay open on their own merits
- [Hotel Fremont pro-parity findings](hotel-fremont-pro-parity-findings.md) — tiny-lettering and satin-border-fragmentation are real and already caught by existing preflight checks; forced_class=flat makes gradient misclassification WORSE not better; "missing backfill" was a render-fidelity misread, underlay is really there
- [Font pipeline silent failures](font-pipeline-silent-failures.md) — the font library's defects all report healthy numbers; 55→85 fonts, six silent defects, a tier gate that was never run, and the `[A-Za-z0-9]` Latin-assumption smell
- [Cache seam, photo floor, environment betrayals](cache-seam-photo-floor-2026-08-22.md) — 2026-08-22 marathon: the Drive photo channel is measured-broken (binary corruption) and unresolved; downloads lie under the proxy (verify Content-Length); the satin floor's defect and disproof populations BOTH classify gradient so only the toggle lane gates them; the blend tier's real off-switch is the speckle gate, not the r² floor
- [The first real photographs](first-real-photos-2026-08-23.md) — 2026-08-23: four family photos found four defects no fixture could reach (7.4 MP OOM; `select_palette` looping forever on two bit-identical spools under an absolute 1e-9 epsilon; preflight condemning correct thread-paint against the tatami target; a 12-cone list sewing 31–35 spools). Region-half palette binding landed, shade half is Kent's call and blocks v1. The funded speckle A/B is answered: near-inert. `pkill -f` matches its own wrapper; give every agent its own worktree
- [The renderer that made the real defect visible](thread-render-and-background-stitches-2026-08-24.md) — 2026-08-24/25: every sheet before this showed VECTOR PROOFS, so no tonal ruling Kent was ever asked for could see the thing being judged. The first thread render found it in one look: 72.2% of a design sewed background rembg had already removed, 91.4% of it in the ONE FDoG detail block, invisible because no acceptance arm had ever set the cutout flag. Fixed, cutout defaulted ON, fallback redirected off the worst arm. Kent's next concern is fill quality WITHIN shapes — streamline covers 0.55-0.59 of its own footprint against the blend tier's 0.99
- [Faces overturn the fill ruling](faces-overturn-the-fill-ruling-2026-08-25.md) — 2026-08-25 evening: lane A answered "a photo sews FILLED" on an owl and two landscapes, three PRs shipped on it, then four real portraits INVERTED it — filled quantizes a face to one skin field and the eyes vanish; on one the person disappeared entirely. Faces TABLED on a capability blocker, not a non-goal. Every MECHANICAL result survived (the 3.5 raggedness cutoff lands in a real gap on all four faces). Real photographs are the LOWEST unique_color_mass in the corpus — below every gradient logo — so stage 0's colour signals cannot detect them, but EXIF camera tags and the already-shipped YuNet detector both score 4/4 vs 0/9. Two defects in the same day's own shipped code, both found by outside pressure not self-review: the border area-share floor INVERTS with design size, and a thin smooth ring was refused. The decode ceiling is service-only; a direct run_stages() on a 7.4 MP photo is unprotected. The digitizer determinism canary flakes ~1 run in 12 on gradient_smoothness and is COSMETIC — 16.5 million times too small to flip a class; don't spend an hour on it twice
- [The Studio's display layer](studio-display-layer-2026-08-25.md) — 2026-08-25: four UI passes found defects 787 green specs could not see, including a primary CTA rendering white-on-white on every wizard step and a canvas menu creating invisible elements. Drive the app in a browser and read COMPUTED styles; a cascade bug is invisible in the rule you are reading. A regression test proves nothing until you have watched it go red — the first simulator spec passed with the bug re-introduced. `var(--x, fallback)` with an undefined name is a silent bespoke value (3 such names, 24 call sites). `paint()` reads `simActive` via `stopSim()`, so any canvas resize while the simulator starts kills it. Kent merges ~1 min after opening, usually before CI: expect stranded commits and a fresh PR
- [Why the letters are botched](letterform-fidelity-2026-08-26.md) — 2026-08-26: FOUR mechanisms, none of them alone. Kent found the one the 13-agent workflow missed entirely: `satin_shape()` takes NO angle argument, every cross is that shape's own spine tangent, so each letter picks its own angle and nothing can make a word agree — `fill_angle_deg` exists for FILL only. Coverage and fidelity are BLIND to it (a wrong-angle letter scores a perfect IoU). Then: pull comp is a round-join dilate applied BEFORE decomposition whose min-feature guard is scoped to `poly.interiors`, so the E's arm slots close to 0.336mm while O's counters are protected; `_prune_spurs` destroys the branch node its own docstring promises to keep, welding the N's diagonal to its stem through a 108° fold (ablation: N bare 12.7→4.1%); and "AND DRONE" at 0.55mm strokes is below what satin can render at all — coverage or shape, not both. **Bare-fabric coverage scored the visibly-deformed H at "1.9% bare, fine" — the instrument is why this survived.** Two irresistible fixes are traps: `_SPLIT_TURN_DEG 90→70` reds a golden whose own rule says the change is wrong, and a mitre join is gate-1 blocked. Read before touching stage 5 or stage 6
- [Art-fidelity self instrument, rebuilt from its artifact](artfidelity-self-rebuild-2026-08-27.md) — 2026-08-27: a cloud session's whole output (instrument, tests, CI job, parameter search) was lost UNPUSHED when its container went; the published validation artifact was the only surviving record and turned out to be enough — the composite weights came back EXACTLY by least squares over its 14 published rows (0.40/0.25/0.35, max residual 0.08 = its own rounding). Then the colour component was rebuilt wrong TWICE, both times structurally dead for one reason: **the floor was something the pipeline had already optimised, so the excess was zero by construction** (best-loaded-spool is what stage 4 snaps to; best-single-spool-for-the-region is what stage 4 chooses). The live reading floors each PIXEL at its own best spool, and the subtraction must happen BEFORE the median, not after. Unit tests never noticed — a component returning 1.000 everywhere is well-behaved; the corpus run caught it because the artifact's column varied and the rebuild's did not. Also: preflight's "pooling was the original sin" ruling was over-applied and pushed the rebuild toward the dead reading — it forbids collapsing pixels to a summary COLOUR, not aggregating per-pixel ERRORS. Read before touching artfidelity_self.py or quoting its numbers against the artifact
- [Curve fidelity from the stitch path](curve-fidelity-from-stitch-path-2026-08-27.md) — 2026-08-27: curve fidelity is NOT readable from a raster and IS readable from `plan.iter_runs()`; the rejected turning-concentration measure was right and its input was wrong (a rasterised circle reads more angular than a 40-gon). Monotonic on the n-gon ladder at 0.5/1.0/2.0 mm, dead at 3.0 because the needle is as coarse as the polygon. Two numbers that fail differently: `turn_gini` ranks severity but a rounded rectangle reads like a 20-gon, `roughness_deg` detects regardless of mixed geometry but saturates and reverses. Sagitta was tried and cut (it measures sampling fineness). It resolves `simplify_tol_mm`, which the raster's 0.35 mm floor could not — but `becker`'s traces go 50→77 between arms, so read `traces`/`curve_vertices`/`corner_vertices` beside every delta. Read before quoting a smoothness number
