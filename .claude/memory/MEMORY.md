# Memory Index

- [Fritsch's Stitches](fritschs-stitches.md) — Kent's company is an embroidery business; assume embroidery domain, ignore wheel/tire context
- [EMB Bot Digitizer](emb-bot-digitizer.md) — embroidery digitizer tool at <repo-root>; 2026-07-29 market-parity-vs-Ember launch roadmap; quality engine state + "flat art in, pro out" rule
- [Ember architecture](ember-architecture.md) — competitor teardown: 2 Next.js apps + iframe, PixiJS/MobX/WASM, no ML in auto-digitize; client-side codec usable as DST reference
- [Ember feature teardown](ember-feature-teardown.md) — 2026-08-09 manual/pricing teardown; fill-pattern lib + Bridge app are biggest gaps; handoff doc in EMB-Bot/docs
- [Fill density convention](fill-density-convention.md) — Law 19 settled: pro density is measured between same-direction rows, so our tatami is 2x light; satin is correct
- [DST codec axis discrepancy](dst-codec-axis-discrepancy.md) — EMB-Bot's JS DST bit table is transposed vs pyembroidery/Tajima standard; unresolved, needs a sew-out
- [Ink/Stitch research](inkstitch-research.md) — 2026-08-10 gap/corroboration sweep; pystitch is 5th DST-axis source + MIT swap candidate; GPL-3.0 caveat on porting
- [Pro trim threshold](pro-trim-threshold.md) — corpus says the pro floats up to 16.1mm uncut vs our trim_at_mm 3.0, but the cut/float distributions overlap: distance is not the decision variable
- [Real-artwork parity](real-artwork-parity.md) — 2026-08-15/16: honest baseline 42.5, a pro scores only 75-84 against a pro, and synthetic references flattered the engine in four places
- [Satin gate attribution](satin-gate-attribution.md) — the DT regularity term loses the pro's satin ground; promotion on `explained` moved the corpus 45.8 to 48.1, and the sub-1mm width floor is disproved for flat art
- [Windows goldens fail locally](windows-goldens-fail-locally.md) — per-fixture golden divergence: Windows fails a different three than CI deselects; main is green, never re-capture a golden locally; OCR tests skip without tesseract
- [Digitizer local env](digitizer-local-env.md) — venv editable since 2026-08-17 (stale site-packages shadowing fixed); system tesseract missing, so the real-read OCR tests skip locally (skipif since PR #165) and run in CI
- [Satin extremity drop and coverage check](satin-extremity-drop-and-coverage-check.md) — enthusiast_logo's satin brackets silently drop tabs/corners despite correct outlines; preflight's new ARTWORK_UNCOVERED check catches it, but ground truth needs polygon ∩ ink — either mask alone has its own false-positive class
- [Real-artwork trim truth](real-artwork-trim-truth.md) — 2026-08-22: on Kent's REAL client logos the only large trim lever is `chain_links` (−33%, and FEWER stitches), gate-1 frozen and awaiting his call; PR #205's fill ordering is byte-identical there and every gate-clear alternative is ≤9%. Real logos are satin-dominated, 1–3 fill shapes that never cut. Read before proposing any trim work
- [Sew-out accepted as-is](sew-out-accepted-as-is.md) — Kent ruled 2026-08-21 the physical sew-out is accepted as-is, NOT a queued to-do; dependent scores are permanently `pending sew-out` and ROADMAP gate 1 is a standing refusal — but it does NOT decide the DST codec fix or `split_tonal_regions`, both of which stay open on their own merits
- [Hotel Fremont pro-parity findings](hotel-fremont-pro-parity-findings.md) — tiny-lettering and satin-border-fragmentation are real and already caught by existing preflight checks; forced_class=flat makes gradient misclassification WORSE not better; "missing backfill" was a render-fidelity misread, underlay is really there
- [Font pipeline silent failures](font-pipeline-silent-failures.md) — the font library's defects all report healthy numbers; 55→85 fonts, six silent defects, a tier gate that was never run, and the `[A-Za-z0-9]` Latin-assumption smell
- [Cache seam, photo floor, environment betrayals](cache-seam-photo-floor-2026-08-22.md) — 2026-08-22 marathon: the Drive photo channel is measured-broken (binary corruption) and unresolved; downloads lie under the proxy (verify Content-Length); the satin floor's defect and disproof populations BOTH classify gradient so only the toggle lane gates them; the blend tier's real off-switch is the speckle gate, not the r² floor
- [The first real photographs](first-real-photos-2026-08-23.md) — 2026-08-23: four family photos found four defects no fixture could reach (7.4 MP OOM; `select_palette` looping forever on two bit-identical spools under an absolute 1e-9 epsilon; preflight condemning correct thread-paint against the tatami target; a 12-cone list sewing 31–35 spools). Region-half palette binding landed, shade half is Kent's call and blocks v1. The funded speckle A/B is answered: near-inert. `pkill -f` matches its own wrapper; give every agent its own worktree
- [The renderer that made the real defect visible](thread-render-and-background-stitches-2026-08-24.md) — 2026-08-24/25: every sheet before this showed VECTOR PROOFS, so no tonal ruling Kent was ever asked for could see the thing being judged. The first thread render found it in one look: 72.2% of a design sewed background rembg had already removed, 91.4% of it in the ONE FDoG detail block, invisible because no acceptance arm had ever set the cutout flag. Fixed, cutout defaulted ON, fallback redirected off the worst arm. Kent's next concern is fill quality WITHIN shapes — streamline covers 0.55-0.59 of its own footprint against the blend tier's 0.99
- [Faces overturn the fill ruling](faces-overturn-the-fill-ruling-2026-08-25.md) — 2026-08-25 evening: lane A answered "a photo sews FILLED" on an owl and two landscapes, three PRs shipped on it, then four real portraits INVERTED it — filled quantizes a face to one skin field and the eyes vanish; on one the person disappeared entirely. Faces TABLED on a capability blocker, not a non-goal. Every MECHANICAL result survived (the 3.5 raggedness cutoff lands in a real gap on all four faces). Real photographs are the LOWEST unique_color_mass in the corpus — below every gradient logo — so stage 0's colour signals cannot detect them, but EXIF camera tags and the already-shipped YuNet detector both score 4/4 vs 0/9. Two defects in the same day's own shipped code, both found by outside pressure not self-review: the border area-share floor INVERTS with design size, and a thin smooth ring was refused. The decode ceiling is service-only; a direct run_stages() on a 7.4 MP photo is unprotected. The digitizer determinism canary flakes ~1 run in 12 on gradient_smoothness and is COSMETIC — 16.5 million times too small to flip a class; don't spend an hour on it twice
- [The Studio's display layer](studio-display-layer-2026-08-25.md) — 2026-08-25: four UI passes found defects 787 green specs could not see, including a primary CTA rendering white-on-white on every wizard step and a canvas menu creating invisible elements. Drive the app in a browser and read COMPUTED styles; a cascade bug is invisible in the rule you are reading. A regression test proves nothing until you have watched it go red — the first simulator spec passed with the bug re-introduced. `var(--x, fallback)` with an undefined name is a silent bespoke value (3 such names, 24 call sites). `paint()` reads `simActive` via `stopSim()`, so any canvas resize while the simulator starts kills it. Kent merges ~1 min after opening, usually before CI: expect stranded commits and a fresh PR
- [Why the letters are botched](letterform-fidelity-2026-08-26.md) — 2026-08-26: FOUR mechanisms, none of them alone. Kent found the one the 13-agent workflow missed entirely: `satin_shape()` takes NO angle argument, every cross is that shape's own spine tangent, so each letter picks its own angle and nothing can make a word agree — `fill_angle_deg` exists for FILL only. Coverage and fidelity are BLIND to it (a wrong-angle letter scores a perfect IoU). Then: pull comp is a round-join dilate applied BEFORE decomposition whose min-feature guard is scoped to `poly.interiors`, so the E's arm slots close to 0.336mm while O's counters are protected; `_prune_spurs` destroys the branch node its own docstring promises to keep, welding the N's diagonal to its stem through a 108° fold (ablation: N bare 12.7→4.1%); and "AND DRONE" at 0.55mm strokes is below what satin can render at all — coverage or shape, not both. **Bare-fabric coverage scored the visibly-deformed H at "1.9% bare, fine" — the instrument is why this survived.** Two irresistible fixes are traps: `_SPLIT_TURN_DEG 90→70` reds a golden whose own rule says the change is wrong, and a mitre join is gate-1 blocked. Read before touching stage 5 or stage 6
- [Art-fidelity self instrument, rebuilt from its artifact](artfidelity-self-rebuild-2026-08-27.md) — 2026-08-27: a cloud session's whole output (instrument, tests, CI job, parameter search) was lost UNPUSHED when its container went; the published validation artifact was the only surviving record and turned out to be enough — the composite weights came back EXACTLY by least squares over its 14 published rows (0.40/0.25/0.35, max residual 0.08 = its own rounding). Then the colour component was rebuilt wrong TWICE, both times structurally dead for one reason: **the floor was something the pipeline had already optimised, so the excess was zero by construction** (best-loaded-spool is what stage 4 snaps to; best-single-spool-for-the-region is what stage 4 chooses). The live reading floors each PIXEL at its own best spool, and the subtraction must happen BEFORE the median, not after. Unit tests never noticed — a component returning 1.000 everywhere is well-behaved; the corpus run caught it because the artifact's column varied and the rebuild's did not. Also: preflight's "pooling was the original sin" ruling was over-applied and pushed the rebuild toward the dead reading — it forbids collapsing pixels to a summary COLOUR, not aggregating per-pixel ERRORS. Read before touching artfidelity_self.py or quoting its numbers against the artifact
- [Curve fidelity from the stitch path](curve-fidelity-from-stitch-path-2026-08-27.md) — 2026-08-27: curve fidelity is NOT readable from a raster and IS readable from `plan.iter_runs()`; the rejected turning-concentration measure was right and its input was wrong (a rasterised circle reads more angular than a 40-gon). Monotonic on the n-gon ladder at 0.5/1.0/2.0 mm, dead at 3.0 because the needle is as coarse as the polygon. Two numbers that fail differently: `turn_gini` ranks severity but a rounded rectangle reads like a 20-gon, `roughness_deg` detects regardless of mixed geometry but saturates and reverses. Sagitta was tried and cut (it measures sampling fineness). Then measured against `edge_smoothness`: `roughness_deg` is NOT edge noise (Spearman 0.028 vs `ragged_mm`, so the two instruments are not redundant), but `turn_gini` is substantially a COMPLEXITY statistic (Pearson −0.763 vs log trace count; two 2-trace designs pin the top at 0.95) — read `roughness_deg` per design and keep gini to the ladder and paired arms. Read before quoting a smoothness number
- [Four thresholds on the wrong population](thresholds-on-the-wrong-population-2026-08-28.md) — 2026-08-27/28: Kent's *"Why is the 'N' running Vertically?"* took four layers to reach and **every layer was the same mistake**: a threshold correct for the population it was calibrated on, applied to one it had never seen. `rescued_small_shape` (0 of 17 regions), `STROKE_CV_MAX` 0.32 (real glyphs score 0.36-0.68), `COHERENCE_FALLBACK_MIN` 0.25 (a pixel field grading skeletons) — **I introduced the third myself, the same day I documented the first two.** The tell: a gate that should find something finding nothing on input that obviously contains it. Second shape, also recurring: `satin_shape(angle_deg=...)` was BUILT on 08-26 and nothing ever set it, so the sewn output never changed; then the first wiring was INERT on the very logo the complaint came from, and only a render caught it — green CI and 9/9 mutations killed with zero effect. Shipped: one house angle per line of lettering on BOTH satin and fill (7 of 11 lettering regions sew as fill, where `best_fill_angle_deg` picks per shape and put two adjacent capitals at 22.5 and 90.0 deg — the actual complaint). Becker: strokes within +/-20 deg of mode **29% -> 51%**, thread **-2.4%**, trims flat. Gate is Rayleigh chance-corrected because NO raw threshold works (rings 0.167 vs lettering 0.197). **Calibrated on two groups from one logo and live on main.** Also: the exterior-notch guard is built/costed/PARKED (reds the chaining benchmark 3.8 -> 6.4 per 1k), the letterform instrument is half rebuilt (worst-stroke, blind to tilt) and the obvious tilt metric was built and REJECTED (ranks a good O worse than the deformed H), and `_prune_spurs` should wait for a working tilt metric because its failure mode IS the tilt defect
- [Kent's eye vs the instruments](kent-eye-vs-instruments-2026-08-27.md) — 2026-08-27: first per-design feedback in Kent's own words (14 notes verbatim in `docs/kent-review-2026-08-27.md`). He puts the stitch-outs at **60% of the way to Ember parity** while artfidelity_self averages 83.7 and preflight 80.0 — and those two agree with each other at only rho=0.405. **ARTFID is a fidelity score and must never be quoted as a quality percentage**; it is blind to craft, which is most of his 40%. He independently named the split: *"Shapes are accurate but smoothness is not."* Phase 1's exit condition may be unreachable with a fidelity-only metric. Two themes, both mattering equally to him: smoothness (8/14, itself TWO complaints — edge noise AND curves sewn as polygons) and whole elements missing (7/14; both his "out of place" marks are the two that lost an element). `ARTWORK_UNCOVERED` fired on 1 of those 7 and reported 0.0 mm2 on the rest — it is scoped to shapes the design already sews, so an element dropped before stage 3 is invisible to it. Built `tools/dropped_elements.py` (works: 99.1% lost on the logo Kent called "5% completed at most") after THREE element definitions failed, and `tools/edge_smoothness.py` (half works). **Curve fidelity is not readable from a raster — measured: a rasterised circle reads more angular than a 40-gon at every resample step.** The trap that cost the most: measuring pictures of stitches instead of the stitches; next instrument reads `plan.iter_runs()`. Also here: two unfixed engine defects (summit_badge's half-removed background — PR #276's body wrongly says the engine is correct there; and stage1_prep coupling BACKGROUND_ABSENT to a colour threshold), and the artifact self-republishes so NEVER write it from a check-in
- [The first physical stitch-out](first-physical-sewout-2026-09-01.md) — 2026-09-01: Kent sewed the Instagram icon, 6/10; all four findings mapped (glyph-first ordering, shade-patch quilt, 229-st lens re-entry, 8-11.5mm tail jump-chains); the 0.40-pitch density story RETRACTED against the sewn file's measured 0.18-0.19 mm — see-through is seams/borders/sequencing, not pitch; borders-last + quilt-cleanup approved as task cards (quilt/hopping remainder TABLED by Kent 2026-09-02 — MASTER_SCOPE queue 12; borders-last itself shipped, PR #302); gate 1 stands, card block 2 is the density instrument
- [Does the yardstick agree with Kent's eye?](../../docs/yardstick-vs-kents-eye-2026-08-28.md) — 2026-08-28: the phase-1 exit question, attempted and NOT settled. The proposed chance-corrected agreement was abandoned before running — Kent never gave a ranking, only verdicts ON the instrument's ranking (6 of 8 rows), so there is nothing to correlate. Replaced with a separation test. **Element COUNT matches his judgement, element AREA does not** — `ribbon_curve` (endorsed) loses more area than `becker` (flagged), and `bg_uncertain` (endorsed) has the largest single lost element; he names elements, never areas, so weight `lost` not `lost_frac`. `artfid` alone now separates his flagged from endorsed, but that is a CONFOUND not a validation: his verdicts were on pre-#282/#283 stitch-outs that no longer exist. n=6, chance 1/15. **Settling it needs fresh per-design verdicts on CURRENT output.** Read before claiming the yardstick agrees with Kent
- [Worktree add with empty var wipes cwd](worktree-add-empty-var-wipes-cwd.md) — a subagent's `git worktree add "$UNSET"` from inside a lane emptied its checkout (borders-last review, PR #300); branch/commits survive, repair is one `worktree add <path> <branch>` from the primary
- [Worktree venv and baselines](worktree-venv-and-baselines.md) — worktrees have no .venv (use main checkout's python, cwd shadows); main checkout sits on feature branches, never a baseline — temp-worktree the merge-base instead
- [The baseline was sound, and a shallow clone said otherwise](baseline-attribution-and-studio-ui-2026-09-02.md) — 2026-09-02: I published "the scorecard baseline is incoherent" off a SHALLOW clone, whose graft root answers `git log -1 -- <path>` as every file's last author while wearing a real merge's subject line; three independent checks corroborated one stale snapshot. `git fetch --unshallow` (219 → 1390 commits) put the real capture at `4f7d80f3` 2026-08-12, **206 merges earlier**, and **38 of 38 rows then reproduced** — fully retracted. Check `git rev-parse --is-shallow-repository` before attributing anything to a commit. Every flagged mover is now attributed (`2d58da8`, `d2184fa2`, `b37cd808`, `da7fc806`, `070a1136`, the preflight link defect, `d3f3c547`); the photo lane reproduces bit-for-bit on Linux. Tonal splitting is OUTSIDE gate 3 and Kent re-confirmed it deliberate — the DOCS were wrong, not the code — but its density bill is real and is now **defect 20** (`coverage_max` 4.40 → 7.18 against a 3.5 ceiling), and `effective_split_tonal` had **no off switch** because it ORed flag with class under a test whose name claimed both directions and asserted one. Its BENEFIT stays unmeasurable: `artfidelity_self` refuses all 8 photo rows. Studio side (#317/#318): the picked hoop is finally DRAWN and gates exports — **the stock Tote / Full Back preset is 203.2 mm against a 200 mm hoop, so the confirm fires on shipped artwork and the auto-fit-cap question is open and unasked** — and `cfg.border` could never reach its own default because the app always sent the key. **The cone merge was built, measured inert and REVERTED**: palette slots are not region cones, and Kent has since TABLED the colour question (queue 12)
- [Hotel Fremont fine details](hotel-fremont-fine-details-2026-09-02.md) — 2026-09-02: Kent's five screenshot notes traced. E/L/T weight = hanging-serif corner fan, cured by the house angle — which was NOT FIRING: slab-serif lettering cancels itself in doubled-angle space (nR² 4.7 vs 6.9), the FOURTH threshold-on-the-wrong-population instance. PR #321 adds a four-fold second reading, DEFAULT OFF because enthusiast @ 93 mm piles the N and reds the chaining benchmark 2.43 → 4.62/1k (resampled at 4 px because raw skeleton steps carry a four-fold raster grain — annuli read 0.160 at exactly 45°; plus an effect floor because a biased null defeats significance) and the 45° BISECTOR (house = 0 scrambles every horizontal through the span clamp; "axis + 45" flips 45/134 across the mod-90 wrap). Fires on drone's THERMAL too — the letters Kent called not clean. O not round = `simplify_tol_mm` 0.2 makes the counter a 9-gon ±0.25 mm; in-fill = 570 mm of travel over finished rows, NOT the render (pro tatami is smooth in the same renderer); Wisconsin = 15-gon, 14 rows. Ring fixtures are degenerate (2.5–3.5 mm remnants)
- [Round curves](round-curves-2026-09-03.md) — 2026-09-03: defect 22 built OFF as `curve_turn_deg` (midpoint re-read of each DP edge, 1 px floor; Fremont's counter 9 → 33 vertices, 47° → 17°); found under it defect 25, fill stitches halved by float dust at the 3.0 mm threshold (8–10% of the stitches on whitebg, Fremont, sunset; `tools/fill_dust.py`). Dust FIXED the same day (PR #328); near-floor lettering exempt per ring (after review); FLIPPED ON 2026-09-03 gated to four pixels of tolerance (`_CURVE_MIN_EPS_PX`) after `tools/curve_tiers.py` showed the 1-px floor reading texture below ~16 px/mm and two classifier tier flips.
- [Classifier stability](classifier-stability-2026-09-03.md) — 2026-09-03: MEASURED NEGATIVE — 5 of 219 satin/fill verdicts flip under boundary detail (4 on threshold edges); eight cures (spur pruning ×3, sewing rule, hybrid, raster smoothing ×2, regularity band) all leave 3–12 flips and change 2–48 shipped verdicts; no engine change; `tools/ribbon_stability.py`.
- [Rail dents](rail-dents-2026-09-03.md) — 2026-09-03: defect 23 FIXED; the open half BUILT as `satin_rails_follow_edge` (OFF, Kent's flip: bare satin area Becker 8.6 → 5.8% for +10–17% thread; the inside-gap metric was the guard, not the rail model) — `place` stepped an overshooting rail in by 15% however small the overshoot (70–90% sub-pixel); now on the edge along its own normal, micron containment tolerance, taper zones and caps keep the ladder; rail jitter halves on every fixture. The recorded "in every golden" was a synthetic-bar artefact (measured negative); the symmetric-offset model's inside gaps stay open.
- [Stitch-angle convention](stitch-angle-convention-2026-09-03.md) — 2026-09-03: Kent asked for a rule instead of a 45/135 choice. Measured: pro file axial columns 4.7–7.9° off perpendicular, diagonals 15.9° (p90 43°); 86 shipped fonts stems 1.8°, bars 3.0°, diagonals 18° (N/A/V/K 29.5°, 58% leaning toward the stems' cross); one angle per glyph is 5 of 64 fonts. Expired Goldman rule for ≥45° corners (through-member + butt-join, diagonal runs through) and Pulse density-under-lean (perpendicular pitch = spacing·cos θ → the 48° N was 1.41× dense, the whole 45° Fremont too). Rule proposed: house = stems' perpendicular, perpendicular fallback (no ±45 side flip), lean cap 30°, spacing/cos θ, Goldman join; no bisector, no convention constant
- [Fill travel under cover](fill-travel-under-cover-2026-09-03.md) — 2026-09-03: defect 21 FIXED, default ON by Kent's flip (2:25 exposed-vs-cut weight ratified; Fremont pays 5 trims for 196 mm less visible travel). Routing alone did NOTHING (the inset ring runs through sewn columns; exposure is decided by the ORDER) — the cover-aware column order + routing through the unsewn remainder is the lever: Fremont fill-phase exposed 286 → 92 mm, gaulke 209 → 8, sunset trims 53 → 30. Flag off md5-identical. Cost +49% time on the 263-run photo fill; `_ring_route` was rebuilding its arc table per call (34.8 s of a 90 s profile) — cached. Review caught an unclipped endpoint allowance letting covered routes leave the shape — fixed with hard containment. whitebg goldens re-pinned via the pre-change worktree, towel stays the known red
- [Fine-lettering design review](fine-lettering-design-review-2026-09-03.md) — 2026-09-03: the "Embroidery 101" write-up IS Laws 45–58; seven half-built rules found, Kent chose three plus the 5 mm underlay rung. Defect 24's MECHANISM fixed in both engines (a hairline STRETCH sews as a 3-pass bean along its spine; per station, because Goldman-joined members keep their junction crosses), the tier question stays on card block 5. A bean earns its place only where the ART has ink — pull comp grows 0.04 mm vectorization needles into strokes (Fremont's tick; the art-polygon floor took +276 stitches to +12). Convert-to-text finally sees ordinary lettering, third attempt: two doors clustered in two ROUNDS (rescued first, unchanged code, so byte-identity is structural), the shield star out on a one-ink CIEDE2000 link at 20 (thread identity disproven, inter-glyph gap measured dead), e2e per cluster, cost measured quiet (+0.9 s enthusiast, service test 12.4 s of 60; a thread pool over tesseract is a measured 7x NEGATIVE; tesseract's own OpenMP team is why the 60 s service test dies under `-n auto` — pinned to one thread the test runs 12.1 s under three CPU hogs vs 32.7, the likeliest root cause of 10ae9cc's CI timeout) Follow-up: Bold's counter guard — the weight is held per rail where a rail FACES another across a gap the cross floor cannot spare (0.72 mm eye 0.50 vs 0.42 unguarded; 0.36 mm untouched vs 0.06), pull comp never held; junctions hold too, so no Studio note; on hairline scripts bold now means "thicker where there is room" (mai_en_fleur 1,393 vs 3,043). Then short stitches (Law 53) mirrored from the Python guard, width-gated by the cross floor: geneva S 43% → 0% bunched, counts identical; faces at the floor keep bunching by design
- [The pro's files refute the scale-limit story](pro-files-refute-scale-limit-2026-09-03.md) — 2026-09-03: `Embroidery Files.zip` holds the pro's DST/PES (Fremont, Becker, more), not just simulations; the Fremont patch sews THE / EST 1895 / EAT|STAY|PLAY legibly at 92.5 mm as 0.8 mm satin, so "physical-scale limitation" is retracted — our loss is SLIC blobs + bean-under-floor; `dropped_elements` reads 0.2% on text Kent calls lost (blind to illegible); Becker fixture is 1.5 px/mm and Bridge Bar 3.5 px/mm with no low-res warning; Bridge's 13 colours = overflow +3 and re-snap escape (not per-population); fill travel under cover leaves 32–39% exposed
- [The five sew-out findings, density read as rows](sewout-five-points-2026-09-03.md) — 2026-09-03: pro fills are 0.14–0.17 mm adjacent-row pitch as the union of passes (`tools/row_pitch_union.py`), ours 0.40 — 2.4–2.8×, Law 19 conservative; `fill_pitch.py` per-pass over-reads a pro tatami ~2.7× (09-02 "does not support half" retracted); seams already underlap 0.25 mm (untested on cloth, no card block); sew-out palette was operator threading; blocky bands are SLIC splitting a smooth ramp, not the blend; trims are 3 DST jumps, check the machine
- [Fill row spacing settled at 0.15](fill-row-ruling-2026-09-03.md) — 2026-09-03: Kent ruled the pro's pitch; FILL_ROW_MM 0.40 → 0.15, coverage grader re-based in fill layers (one fill = 2.67 units, warn 6.67 / block 9.33 — pre-date numbers are 2.67× smaller), coverage map sampled 8× across the ribbon (two samples misread 0.15 by 7%), contour rings held at 0.40 (gated tier), fills 2.67× stitches; JS engine densityMm defaults still 0.45/0.4 — next PR
- [Seams: instrument, card block 6, blend underlap](seams-2026-09-03.md) — 2026-09-03: `tools/seam_underlap.py` reads sewn overlap per colour pair; the rule (pull + 0.25 earlier-under-later) holds on synthetic logos but Fremont carries 0.24 mm mean over 1,549 mm of seams because hole-held small shapes get no tongue; card block 6 (0 / 0.25 / 0.5 / 1.0) decides the number; blend bands now underlap by overlap_mm

